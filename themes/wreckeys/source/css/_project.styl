.projects {
  padding-bottom: 0;
}

.project-list {
  margin: 2em 0 0 0;

  li {
    position: relative;
    padding-left: 1.2em;
    margin-top: 1em;
  }

  li::before {
    content: "➤ ";
    position: absolute;
    margin-left: -1em;
    top: 0;
  }
}

.post-date, .post-short {
  opacity: 0.7;
}
